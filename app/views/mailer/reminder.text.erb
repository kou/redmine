<%= l(:mail_body_reminder, :count => @issues.size, :days => @days) %>:

<% @issues.each do |issue| -%>
* <%= "#{issue.project} - #{issue.tracker} ##{issue.id}: #{issue.subject}" %>
<% end -%>

<%= @issues_url %>

<%= call_hook(:view_mailer_reminder_bottom,
              :issues => @issues) %>
